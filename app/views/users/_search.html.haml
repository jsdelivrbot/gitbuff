.ui.search
  = form_tag(user_search_path, id:'search_form') do
    .ui.fluid.action.left.icon.input
      = text_field_tag :username, '', placeholder: 'Kullanıcı adını giriniz...', type: "text", class: 'prompt'
      %i.github.icon
      .ui.button#search
        Ara

:javascript
  $('.ui.search')
    .search({
      apiSettings: {
        url: '//api.github.com/search/users?q={query}&access_token='
      },
      fields: {
        results : 'items',
        title   : 'login',
        image   : 'avatar_url',
        url     : 'login'
      },
      minCharacters : 3
    })
  ;
  $("#search").click(function() {
    $("#search_form").submit();
  });
